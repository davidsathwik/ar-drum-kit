{
  "name": "ar-drum-kit",
  "version": "1.0.0",
  "private": true,
  "description": "AR webcam drum kit — in-air percussion with customizable pads",
  "type": "module",
  "scripts": {
    "test": "node --test \"tests/*.test.mjs\"",
    "serve": "python -m http.server 8000"
  }
}
